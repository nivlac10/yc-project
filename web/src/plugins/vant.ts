import { Button, Swipe, SwipeItem, Tabbar, TabbarItem, Lazyload, Icon, NoticeBar, Switch, Checkbox, CheckboxGroup } from 'vant';
export default function (app) {
    app.use(Button).use(Swipe).use(SwipeItem).use(Tabbar).use(TabbarItem).use(Lazyload).use(Icon).use(NoticeBar).use(Switch).use(Checkbox).use(CheckboxGroup)
}